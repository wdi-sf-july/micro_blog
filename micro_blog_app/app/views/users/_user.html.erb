<div>
  <div class="row">
    <div class="col-xs-12">
      <h2> <%= link_to  user.first_name + " " +  user.last_name, user_posts_path(user.id) %></h3>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-md-4">
      <img src="<%= user.image_url %>" class="img-responsive img">
    </div>
    <div class="col-xs-12 col-sm-6 col-md-4">
    <%= button_to "New Post", new_user_post_path(@user.id), method: :get, class: "btn btn-primary" %>
  </div>
</div>
