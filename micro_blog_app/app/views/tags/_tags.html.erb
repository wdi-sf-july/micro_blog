<div class="row cust-con" %>
  <div class="col-xs-offset-1 col-xs-10" >
    <div class="row">
      <% tags.each do |tag| %>
        <div class="col-xs-6 col-sm-3">
          <%= link_to tag.name, tag_user_path(user.id, tag.id) %>
        </div>
      <% end %>
    </div>
  </div>
</div>
