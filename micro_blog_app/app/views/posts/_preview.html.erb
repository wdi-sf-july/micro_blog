<div class="row cust-con">
  <h3 class="col-xs-offset-1 col-xs-10 col-sm-offset-2 col-sm-8"><%= link_to post.title.titleize, user_post_path(user.id, post.id) %></h3>
  <div class="col-xs-offset-8 col-xs-4">    
    <%= button_to "Delete", user_post_path(@user.id, post.id), method: :delete, class: "btn btn-danger" %>
    <span><%= post.created_at.strftime("%b %d, %Y %l:%M%P") %></span></div>

</div>
